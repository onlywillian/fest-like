import styled from 'styled-components';

export const Button = styled.button`
  padding: 20px;
  background: #009;
  color: white;
  outiline: 0;
  transition: .5s;

  &:hover {
    background: #004;
  }
`